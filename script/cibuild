#!/bin/sh

set -e

bundle exec jekyll build

# Internal links only — checking external ones makes CI fail whenever an
# unrelated speaker's homepage happens to be down.
bundle exec htmlproofer ./_site --disable-external --allow-hash-href
